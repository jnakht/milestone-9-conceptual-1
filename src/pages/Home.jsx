import { useContext } from "react";
import { AuthContext } from "../providers/AuthProvider";

const Home = () => {
    return (
        <div>
            this is home
        </div>
    );
};

export default Home;